# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0

# API and HTTP
httpx>=0.25.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
aiofiles>=23.0.0

# Database (optional, for production)
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# Redis (optional, for queues)
redis>=5.0.0

# Vector similarity (optional, for embeddings)
numpy>=1.24.0

# Image processing (for screenshots)
Pillow>=10.0.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
aiohttp>=3.9.0
black>=23.0.0
flake8>=6.0.0
