# 🚀 Быстрый запуск упрощенного детектора

## ✅ Решение проблемы

Создан **SimpleFaceDetector** - упрощенная версия без MediaPipe, которая работает только с Canvas API.

## 🔧 Что изменилось

1. **Создан новый файл**: `face_detector_simple.js` 
   - Работает без внешних библиотек
   - Использует детекцию движения через сравнение кадров
   - Определяет присутствие пользователя (есть/нет)

2. **Обновлен index.html**: Заменена загрузка `face_detector.js` на `face_detector_simple.js`

## 📋 Как работает

### Принцип детекции:
- Анализирует яркость изображения (проверяет, не закрыта ли камера)
- Сравнивает кадры и определяет движение
- Если долго нет движения → предупреждение (возможно пользователь ушел)
- Если нет изображения → критическое событие (камера закрыта)

### Возможности:
✅ Детекция присутствия пользователя
✅ Автоматические скриншоты при подозрительных событиях
✅ Временные метки и аннотации
✅ Загрузка скриншотов на сервер
✅ Статистика и отчеты

### Ограничения:
⚠️ Не определяет точное количество людей (только есть/нет)
⚠️ Требует движения для подтверждения присутствия
⚠️ Менее точен чем MediaPipe

## 🎯 Запуск

### 1. Обновите страницу в браузере
```bash
# Откройте или обновите
http://localhost:8080/index.html
```

### 2. Нажмите "Запустить прокторинг"
- Разрешите доступ к камере
- SimpleFaceDetector начнет работу автоматически

### 3. Проверьте работу
Откройте консоль (Cmd+Option+I) и увидите:
```
[SimpleFaceDetector] Инициализация...
[SimpleFaceDetector] Запрос доступа к камере...
[SimpleFaceDetector] ✅ Инициализация успешна
Разрешение камеры: 640x480
[SimpleFaceDetector] ✅ Детекция запущена (интервал: 3000ms)
```

## 🧪 Тестирование

### Сценарий 1: Нормальная работа
- Сидите перед камерой
- Двигайтесь периодически
- **Ожидается**: Статус "1 👤 в кадре"

### Сценарий 2: Ушли от камеры
- Встаньте и отойдите
- Подождите 15-20 секунд без движения
- **Ожидается**: Предупреждение "⚠️ 0 👤 в кадре"

### Сценарий 3: Закрыли камеру
- Закройте камеру рукой или отклейте наклейку
- **Ожидается**: Критическое событие

## 📊 Статистика

Вся статистика отображается в интерфейсе:
- **Лиц в кадре**: Текущее количество (0 или 1)
- **Критические события**: Камера закрыта
- **Предупреждения**: Пользователь отсутствует
- **Скриншоты**: Автоматически сохраняются

## 🔄 Сравнение версий

| Функция | MediaPipe (face_detector.js) | Simple (face_detector_simple.js) |
|---------|------------------------------|----------------------------------|
| Подсчет людей | ✅ Точный (1, 2, 3...) | ⚠️ Базовый (0 или 1) |
| Зависимости | ❌ Требует CDN | ✅ Без зависимостей |
| Скорость | ⚡ Быстрая | ⚡ Очень быстрая |
| Точность | 🎯 Высокая | 🎯 Средняя |
| Работа офлайн | ❌ Нет | ✅ Да |
| Скриншоты | ✅ Да | ✅ Да |
| Аннотации | ✅ Да | ✅ Да |

## 🛠️ Дополнительно

### Если нужна версия с MediaPipe
Вернитесь к `face_detector.js`:
```html
<!-- Замените в index.html -->
<script src="face_detector.js"></script>
```

### Настройка интервала детекции
```javascript
// В файле face_detector_simple.js, метод startDetection
startDetection(3000)  // 3 секунды (по умолчанию)
startDetection(1000)  // 1 секунда (чаще)
startDetection(5000)  // 5 секунд (реже)
```

### Настройка чувствительности
```javascript
// В файле face_detector_simple.js, свойство motionThreshold
this.motionThreshold = 10;  // По умолчанию
this.motionThreshold = 5;   // Более чувствительный
this.motionThreshold = 20;  // Менее чувствительный
```

## 💡 Рекомендации

1. **Освещение**: Обеспечьте достаточное освещение рабочего места
2. **Движение**: Делайте небольшие движения каждые 10-15 секунд
3. **Положение**: Сидите на расстоянии 50-70 см от камеры
4. **Фон**: Избегайте движущихся объектов на фоне

## 🎉 Готово!

SimpleFaceDetector готов к работе и не требует внешних библиотек.
